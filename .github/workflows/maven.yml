name: Test CI for the AWS-SDK-TEST
on: 
    push:
        branches: 
            - master
            - 'release/*'

jobs: 
    build: 
        runs-on: windows-latest
        
        steps: 
            - name: change to the directory
              run : cd d:\a
            - name: Running the Build Job
              run: mvn package
        
